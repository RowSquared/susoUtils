% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paradata.R
\name{read_many_para}
\alias{read_many_para}
\title{Read and Process Paradata Files}
\usage{
read_many_para(path = NULL, remove.passive = TRUE, parse = TRUE)
}
\arguments{
\item{path}{Character. The directory path containing the \code{paradata.tab} files.}

\item{remove.passive}{Logical. If \code{TRUE}, passive events will be removed from the data. Default is \code{TRUE}.}

\item{parse}{Logical. If \code{TRUE}, the \code{parameters} column will be split into separate columns for easier readability. Default is \code{TRUE}.}
}
\value{
A data.table containing the combined and processed paradata from all \code{paradata.tab} files in the specified directory.
}
\description{
This function reads all \code{paradata.tab} files from a specified directory,
optionally removes passive events, and parses the parameters into a readable format.
}
\details{
This function performs the following steps:
\itemize{
\item Lists all \code{paradata.tab} files in the specified directory.
\item Reads each file into a data.table with specified column classes.
\item Ensures all default columns are present in the data.
\item Optionally removes rows with passive events.
\item Optionally parses the \code{parameters} column into separate columns for better readability.
}
The passive events removed by default are: \code{VariableDisabled}, \code{VariableEnabled}, \code{VariableSet}, \code{QuestionDeclaredInvalid}, \code{QuestionDeclaredValid}.
}
\examples{
\dontrun{
paradata <- read_many_para(path = "path/to/directory")
}
}
